name: Run on release develop

on: 
  workflow_dispatch:
  push:
    tags:        
      - 'v*'

jobs:
  say_release:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Released on ${GITHUB_REF#refs/heads/}!"
