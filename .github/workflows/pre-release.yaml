name: run on prereleased

on:
  workflow_dispatch:
  release:
    types:
      - published
      - prereleased

jobs:
  say_prerelease:
    runs-on: ubuntu-latest
    steps:
      - run: echo "prereleased!"
